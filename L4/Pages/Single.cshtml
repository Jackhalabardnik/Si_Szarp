@page "{image}"
@model L4.Pages.SingleModel
@{
    ViewData["Title"] = "Single";
}
<h1>@Model.Label (prob: @((Model.Probability * 100).ToString("F0"))%)</h1>
<img src="~/images/@Model.Image" class="img-fluid" />
